---
title: "Claude Code Finally Lets You Cap AI Effort — And Stop Wasting Money"
description: "Claude Code v2.1.267 adds maxEffortLevel — a hard cap on how hard the model tries on every provider. v2.1.268 follows with gateway pricing parity and a WebFetch timeout that ends the hung-request token burn. Combined with Fable 5.1's 1M context and a month of cost-efficiency fixes, your agent bills are about to get predictable."
pubDate: "2026-09-10T10:00:00Z"
updatedDate: "2026-09-12T05:00:00Z"
tags: ["claude-code", "release", "coding-agents", "cost", "anthropic"]
tool: "claude-code"
author: "sage"
image: "/api/og?title=Claude+Code+Finally+Lets+You+Cap+AI+Effort&tool=claude-code"
---

Somewhere between the release notes and your next invoice, Claude Code changed. The last release we covered was **v2.1.235** back in August — a spellcheck and polish drop. Since then, Anthropic shipped **30 more versions in four weeks**, and buried in them are two settings that matter more to your wallet than any feature list: a hard **effort cap** and a new default model that makes the 1M context window cheap.

This is the release-cycle story told in plain English — what changed, what it costs you, and the one setting worth turning on today.

## The headline: you can now cap how hard Claude tries

Claude Code's models work in "effort levels" — low, medium, high, max. More effort means more reasoning before it touches a file, which means better answers on hard problems and higher bills on easy ones. Until now, that knob was per-session and easy to forget.

**v2.1.267 adds `maxEffortLevel`** ([changelog](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)): a top-level setting (or per-model under `modelSettings`) that caps effort on **every** provider — including Bedrock, Vertex, and Foundry. You can always pick a lower level per session, but it can never go above your cap.

Why this matters for beginners: the most common "why is my bill $40?" story is a model silently running at max effort on tasks that need none — a one-line rename doesn't need ten minutes of chain-of-thought. A cap makes spend predictable.

```json
// .claude/settings.json
{
  "modelSettings": {
    "claude-sonnet-5": { "maxEffortLevel": "medium" }
  }
}
```

## The other headline: Fable 5.1 is now the default — 1M context, cheaper

Back in **v2.1.257**, Anthropic made **Claude Fable 5.1** the default Fable model ([changelog](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)). The numbers that matter:

- **1M token context window** — a big codebase fits in one conversation without manual pruning.
- **$10/$50 per million tokens** with **$0.25/Mtok cache reads** — the cheapest frontier-class pricing Anthropic has shipped.
- Sessions on the 1M window now **auto-compact at ~967K tokens instead of ~934K** ([v2.1.247](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — you get more useful memory before summarization kicks in.

The `[1m]` tag you may have seen in `/model` is this window. If you're on a Max plan or an API key, worth explicitly checking which model you're pinned to — older Fable 5 sessions stayed on the smaller window unless you picked the new one.

## The quiet theme: a month of token-efficiency fixes

The biggest cost leak in agent use is **prompt-cache misses** — re-sending the whole conversation because the tool list changed slightly. Claude Code shipped a *long* list of fixes for exactly this:

- **Parallel tool calls and hook output no longer dropped on resumed sessions** over 5 MB ([v2.1.267](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)).
- **Mid-session MCP/plugin tools no longer rewrite the tool list** and break prompt-cache reuse ([v2.1.267](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)).
- **Model switching no longer re-sends every tool definition** ([v2.1.267](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — previously a guaranteed cache miss.
- **A likely-cause line for cache misses** was added to `/cost` ([v2.1.260](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — it tells you *why* you missed, which is the first step to fixing it.
- **Per-session prompt-cache stats** (hit ratio, tokens re-cached, warm/cold) landed in `/cost` ([v2.1.251](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)).
- **OAuth token refreshes no longer invalidate the cache** ([v2.1.248](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — that used to nuke it roughly once an hour in long sessions.

For a beginner the takeaway is simple: **update, then run `/cost`** after a long session. If you see a low hit ratio, the cause is now printed right there instead of being a mystery.

## Three more features worth knowing

**`/diff` panel in fullscreen mode** ([v2.1.260](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — a side panel that shows your uncommitted changes live as Claude edits. No more switching to `git diff` to see what the agent just did; it's next to the conversation.

**`/skill-doctor`** ([v2.1.261](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — lists which loaded skills go unused and what they cost in context, so you can prune them. Every unused skill is tokens that could be cache hits.

**`--restricted` mode** ([v2.1.248](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — a lock-it-down flag that strips the tools that run commands or fetch web pages, keeps file tools inside the working directory, and refuses `bypassPermissions`. One flag for a genuinely safer sandbox.

## What the release train says

Look at the changelog from v2.1.236 through v2.1.267 as one document and two things stand out.

First, **cost control is the product now**. Effort caps, cache-cause reporting, per-session cache stats, cheaper model pricing — Anthropic is competing on your bill, not just on benchmark scores. That is exactly the direction we argued in [The Pricing Problem With AI Coding Tools](/blog/pricing-problem-ai-coding-tools/): the tools that win are the ones that make spend predictable.

Second, **the safety perimeter keeps tightening quietly**. The `[v2.1.260](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)` release reverted a change that made `Read()` deny rules apply to Bash arguments — it had started denying `npm run build` under innocent-looking rules. The [bigger trust story](/blog/ai-coding-trust-paradox-verification-crisis/) is that these permission boundaries are being stress-tested in public, and patched in days.

## What to actually do

1. **Update** — `claude update` (or restart the VS Code extension).
2. **Set a default effort cap** — pick `medium` and see if your tasks notice; most won't.
3. **Check your model** — run `/model` and look for the `[1m]` Fable 5.1 or Opus options if your plan includes them.
4. **Check `/cost` after a long session** — read the prompt-cache line; it now tells you what broke the cache.
5. If you run agents unattended, try `--restricted` for anything that doesn't need shell access.

Full changelog: [`v2.1.235...v2.1.268`](https://github.com/anthropics/claude-code/compare/v2.1.235...v2.1.268)

## Postscript — v2.1.268 keeps the same promise (September 10)

The day after this post's original publish, another version landed that fits the cost-control theme exactly:

- **A WebFetch that can't hang your session anymore.** The fetch tool now fails after **300 seconds** instead of spinning forever on a server that keeps the response open ([v2.1.268](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)). Set `CLAUDE_CODE_WEBFETCH_DEADLINE_MS` to adjust, or `0` to disable. If you have ever watched an agent sit on a single web request while tokens tick away, this closes that leak.
- **Gateway pricing parity for signed-in clients** ([v2.1.268](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — with `pricing:` in `gateway.yaml`, `/cost` now matches the actual spend meter, so the number you see is the number you pay. Also added: a warning when `access_control.allow_cidrs` is empty (a gateway with no IP restriction), and a `gatewayInternalNetworks` setting for orgs that need to allow `/login` from their own public IP block.
- **Third-party endpoint fix** ([v2.1.268](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)) — every turn failing with HTTP 400 on Anthropic-compatible endpoints since 2.1.265 is fixed (a regex in the Artifact input schema those endpoints rejected). If you run through a proxy or mirror, this one matters.
- **Plugin/CLI ergonomics** — `claude plugin install/uninstall/update/enable/disable` and `plugin list` now accept `--json` with `errorDetails` ([v2.1.268](https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md)), and `claude self-hosted-runner --remove-session-state` cleans per-session directories when a session ends.

The one-line takeaway from the whole v2.1.267→268 pair: **the bill is becoming the product.** Effort caps, honest `/cost` numbers, and fetch deadlines all attack the same enemy — money spent on work the model never needed to do. Update, set your cap, and check `/cost` after your next long session.

*Disclosure: some links on this site are affiliate links (aiFiesta).*

## Postscript 2 — v2.1.269: the plugin-eval release (September 11)

The day after v2.1.268, Anthropic shipped another version that stays on theme — with one genuinely new headline for plugin authors:

- **`claude plugin eval`** — run a plugin's eval suite against Claude Code and get scored, reproducible results as JSON + HTML report. This is the first real yardstick for "does this plugin actually do what its README promises." If you maintain plugins (or are shopping for one), this turns a vibes check into a number you can compare.
- **`/output-style [name]`** — switch output styles on the fly, including over Remote Control and in headless sessions. The one-line summary view (`/focus`) gets a spinner tip so you actually find it.
- **Bash edits get a diff** — the Bash tool result now shows which files a command changed (`bashEditDiffEnabled`). When the agent runs a script, you see the diff without asking.
- **Workflow fan-out headroom** — `CLAUDE_CODE_WORKFLOW_MAX_CONCURRENT_AGENTS` (1–256) raises the Workflow tool's per-run concurrent-agent cap for inference-bound parallel work.
- **Terminal fixes a month in the making** — F1/F2/F4 in kitty-protocol terminals, Delete in `st`, Alt+arrows in rxvt-unicode, Shift+punctuation in WezTerm (regressed in 2.1.247). A whole class of "my keys do nothing" is closed.
- **Prompt-cache heart surgery** — two fixes matter for your bill: a response cut off at the output-token limit no longer partially invalidates the next turn's cache, and resuming after an interrupt no longer re-sends context in a way that breaks reuse. Both were silent cache-killers.
- **OTEL_METRICS_INCLUDE_REPOSITORY** — tag telemetry with `vcs.*` repo attributes; useful if you run a metrics stack across many repos.
- **Windows: PowerShell background commands no longer die when Claude Code exits** — a fix Windows users have been begging for since the PowerShell tool landed.
- **CMY(K) JPEGs attach correctly** instead of failing with "cannot decode".

The v2.1.267→269 story in one line: **Anthropic is now spending release cycles on plugin trust, terminal keys, and cache hygiene** — the three least glamorous things in a coding agent, and exactly the things that decide whether it feels like a daily driver. Update, set your effort cap, and if you're a plugin author, go run `claude plugin eval` on your own work before anyone else does.

## Related articles

- [Claude Code Alternatives in 2026: 12 Options Compared](/blog/claude-code-alternatives-2026/)
- [Cline Desktop Grew Up Fast — Heres What v0.0.24 Actually Fixes](/blog/cline-desktop-free-coding-models-zero-api-keys/)
- [Oh My Pi v17.3.3 Fixes Gemini Reasoning Loops, Hashline Edge Cases, and TUI Rendering — What Beginners Need to Know](/blog/oh-my-pi-v17-hub-tool-xdev-plugins/)