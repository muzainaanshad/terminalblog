# MUTED: user wants Telegram ONLY in Articles Management format (ops-digest).
# Deploy still happens via Vercel Git integration — this workflow no longer pings Telegram.
name: Deploy Notify

on:
  workflow_dispatch: # manual only — never auto-fires on push

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Telegram muted
        run: |
          echo "Deploy Telegram notify is intentionally disabled."
          echo "Use workflow Ops Digest (Articles Management) for Telegram."
